
<% Object.keys(messages).forEach(function (type) { %>
<div id='messages'>
  <% messages[type].forEach(function (message) { %>
     <div class='alert alert-<%=type%> alert-dismissible fade show' role='alert'> <%=message%> </div>
  <% }) %>

<% }) %>

</div>